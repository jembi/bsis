codecov:
  branch: develop

coverage:
  status:
    project:
      default:
        target: 70%
        branches:
          - master
          - develop
          - dev/*
          - release/*
          - support/*

    patch:
      default:
        target: 70%
        branches:
          - master
          - develop
          - dev/*
          - release/*
          - support/*
